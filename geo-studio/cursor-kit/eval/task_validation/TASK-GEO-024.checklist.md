# Validation — SPEC-GEO-012

- [x] web/ Vite + React + TS 脚手架
- [x] 可见度看板 Dashboard（tenant/brand 输入 + 三指标柱状趋势）
- [x] Vite proxy /api → backend:3000；backend CORS 5173
- [x] `npm --prefix web run build` PASS
- [x] 仅 web + backend CORS 一行（分 SPEC 交付，各 service 独立 task）

**Result**: PASS
