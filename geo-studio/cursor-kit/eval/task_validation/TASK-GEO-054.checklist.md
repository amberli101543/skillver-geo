# Validation — TASK-GEO-054

- [x] `web/src/api.ts` — `Assertion` / `RetestSchedule` 类型 + fetch/add/delete assertions + fetch/update retest-schedule
- [x] `web/src/AssertionsPanel.tsx` — 断言列表、添加（text + 可选 evidence）、删除
- [x] `web/src/RetestPanel.tsx` — 复测开关、间隔小时、保存、上次/下次复测时间展示
- [x] `web/src/Dashboard.tsx` — 竞品库下方集成 AssertionsPanel + RetestPanel
- [x] `web/src/styles.css` — assertion-list / retest-form 样式
- [x] `npm --prefix web run build` PASS

**Result**: PASS
